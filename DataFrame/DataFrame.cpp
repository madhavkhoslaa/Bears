#include"../reader/lazy_read.h"
using namespace std;

class DataFrame{
    public:
        LazyRead Lazy;
        DataFrame(string csvfile){
            //public lazy read the data
            //iterate through rows
            //create n_col number of dynamic arrays with length n_cols gotten from lazy_read
        }

};